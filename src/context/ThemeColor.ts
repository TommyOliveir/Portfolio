import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#800080',
      light: '#F5EBFF',
      contrastText: '#FFFFFF',
    },
    secondary: {
      main: '#FF5733',
    },
  },
});
