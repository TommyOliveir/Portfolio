import { styled } from "@mui/system";

export const HeaderStyled = styled("div")({
  color: "purple",
  display: "flex",
});

export const NavStyled = styled("ul")({
  listStyle: "none",
  display: "flex",
  flexDirection: "row",
  gap: "10px",
});
